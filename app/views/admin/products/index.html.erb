<div class="container">
  <h2 class="my-5 text-center">商品一覧</h2>
  <div class="row">
    <div class="col-12">
      <%= link_to new_admin_product_path, class: "btn btn-secondary" do %>
       <i class="fas fa-plus"></i>
       新規登録
      <% end %>
    </div>

  <table class="table table-borderless">
      <thead class="table-active">
        <tr class="info">
          <th>商品ID</th>
          <th>商品名</th>
          <th>税抜価格</th>
          <th>ジャンル</th>
          <th>ステータス</th>
        </tr>
      </thead>
   <tbody>

     <% @products. each do |product| %>

    <tr>
      <td><%= product.id %></td>
      <td><%= link_to admin_product_path(product) do %><%= product.name %><% end %></td>
      <td><%= product.price %></td>
      <td><%= product.genre.name %></td>
      <td><%= product.is_active %></td>
      <% end %>
    </tr>
    </tbody>
    </table>

    <%= paginate(@products) %>
  </div>
  </div>
</div>

